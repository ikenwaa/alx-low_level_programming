#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: multiple of a and b
 */

int mul(int a, int b)
{
	if ((a >= 48 && a <= 57) && (b >= 48 && b <= 57))
		return (a * b);
}
